name: build

on: [push, pull_request]

jobs:
  build-android_arm64:
    runs-on:  ubuntu-18.04
    
    env:
      TAG: 14.2.0

    steps:
    - uses: actions/checkout@v2

    - name: Build
      run: ANDROID_NDK_HOME=$ANDROID_SDK_ROOT/ndk-bundle ./build.sh build_android_arm64 $TAG
    
    - uses: actions/download-artifact@v2
      with:
        name: shared-library
        path: node-$TAG/out/Release/lib.target

    - uses: actions/download-artifact@v2
      with:
        name: shared-library
        path: node-$TAG/out/Release/node-v$TAG-headers.tar.gz
